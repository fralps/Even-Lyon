<!-- Main jumbotron for a primary marketing message or call to action -->
<div class="jumbotron text-center">
	<div class="container">
		<h1 class="display-4 text-center">Live your best life!</h1>
		<h3>Never do nothing again</h3>
		<p><%= link_to "Create your event", new_event_path, class: "btn btn-primary btn-lg mt-3 mb-0" %></p>
	</div>
</div>

<% events = Event.all %>
<% events.reverse.each do |event|%>

<div class="container border border-muted p-2 mb-4 rounded text-center col col-6">
	<!-- Example row of columns -->
	<div class="row">
		<div class="col">
			<h3><%= event.title %></h3>
			<p><%= event.description %></p>
			<p><%= link_to "View details", event_path(event), class: "btn btn-secondary", role: "button" %></p>
		</div>
	</div>
</div>
<% end %>
